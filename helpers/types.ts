export type User = {
    userID: string;
    username: string;
    email: string;
    firstName: string;
    lastName: string;
    admin: boolean;
    createdBy: "Disadus" | "Gunn.One" | "WATT" | "Standalone";
    password: string;
};
  
